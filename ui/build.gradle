usesMockito = true
usesJunit = true
usesGcontracts = true

dependencies{
	compile project(':api'), project(':dsl')
	testCompile project(':internal')
}

test{
	systemProperty 'java.library.path', project(':native').buildDir.path + '/binaries/'
}