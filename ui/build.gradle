usesMockito = true
usesJunit = true

dependencies{
	compile project(":core"), project(":dsl"), project(":sender")
}

test{
	systemProperty "java.library.path", project(":native").buildDir.path + "/binaries/"
}