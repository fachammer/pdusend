machine:
  pre: 
    - cp $JAVA_HOME/include/jni.h native/src/main/headers
    - cp $JAVA_HOME/include/jni_md.h native/src/main/headers

dependencies:
  override:
    - ./gradlew

test:
  override:
    - ./gradlew test


