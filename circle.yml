dependencies:
  pre: 
    - cp $JAVA_HOME/include/jni.h native/src/main/headers
    - cp $JAVA_HOME/include/linux/jni_md.h native/src/main/headers

test:
  override:
    - ./gradlew test cobertura
  post:
    - ./gradlew distTar distZip installApp
    - cp -r --parents common/build/reports internal/build/reports dsl/build/reports ui/build/reports build/distributions $CIRCLE_ARTIFACTS